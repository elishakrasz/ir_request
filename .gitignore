# Secrets — never commit
.env
.env.*
!.env.example

# Runtime state (delta tokens, run logs)
ingestion/state/
state/

# Solution build artifacts (regenerate via solution/export.sh)
solution/build/

# Python
__pycache__/
*.pyc
venv/
.venv/

# MSAL token caches
.msal*cache*.json

# Windows download metadata
*Zone.Identifier

# Data exports (PII) — never commit
reports/
